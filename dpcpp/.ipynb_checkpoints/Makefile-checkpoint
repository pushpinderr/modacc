# Makefile for GNU Make

default: run

all: run

run: matmult
	./matmul

DPCPP_OPTS = -I${MKLROOT}/include -mkl -fsycl-device-code-split=per_kernel

matrix_mul_mkl: src/matmul.cpp
	dpcpp $< -o $@ $(DPCPP_OPTS)

clean:
	-rm -f matmul

.PHONY: clean run all
